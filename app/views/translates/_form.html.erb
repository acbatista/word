<%= form_for(@translate) do |f| %>
  <% if @translate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@translate.errors.count, "error") %> prohibited this translate from being saved:</h2>

      <ul>
      <% @translate.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :brasileira %><br>
    <%= f.text_field :brasileira %>
  </div>
  <div class="field">
    <%= f.label :inglesa %><br>
    <%= f.text_field :inglesa %>
  </div>
  <div class="field">
    <%= f.label :data %><br>
    <%= f.datetime_select :data %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
